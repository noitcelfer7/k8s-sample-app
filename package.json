{
  "devDependencies": {
    "@types/node": "^22.10.1",
    "typescript": "^5.7.2"
  },
  "scripts": {
    "start": "node dist/index.js",
    "build": "tsc --outDir dist src/index.ts",
    "run": "npm run build && npm run start"
  }
}
